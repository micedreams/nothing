name: nothing_app
description: An app that does nothing.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  fonts:
    - family: Athiti
      fonts:
        - asset: assets/Athiti-Regular.ttf
